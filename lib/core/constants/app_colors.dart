import 'package:flutter/material.dart';

class AppColors {
  // Primary blue used in headers, borders, buttons
  static const primary = Color(0xFF2B5BA8);
  static const primaryLight = Color(0xFFD6E4FF);

  // Accent orange used in buttons, cards, tags
  static const accent = Color(0xFFE8843A);
  static const accentLight = Color(0xFFFFF0E6);

  // Backgrounds
  static const background = Color(0xFFF0F6FF);
  static const white = Colors.white;
  static const scaffoldBg = Color(0xFFF8FAFF);

  // Text
  static const black = Color(0xFF1A1A2E);
  static const darkBlue = Color(0xFF1B3A6B);
  static const grey = Color(0xFF8E8E93);
  static const lightGrey = Color(0xFFE5E5EA);

  // Card/border
  static const border = Color(0xFFD0DDEE);
  static const cardBg = Color(0xFFDFECFF);
}